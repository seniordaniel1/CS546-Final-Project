<section>
    <h1>{{title}}</h1>
    {{#each posts}}
    <article class="post">
        <h2><a href="/posts/{{_id}}">{{userId}}</a></h2>
        <p>{{content}}</p>
    </article>
    {{/each}}
</section>